import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'app.lovable.inkrealm.mirror',
  appName: '1991',
  webDir: 'dist',
  android: {
    allowMixedContent: true,
    backgroundColor: '#0D0D0D'
  },
  icon: 'icon.png'
};

export default config;
